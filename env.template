# =============================================================================
# Jade Frontier Journal — Environment Variables
# Copy to .env and fill in values: cp env.template .env
# =============================================================================

# --- Anthropic (Claude API) ---
# Get key at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# --- Neon (PostgreSQL + pgvector) ---
# Get credentials at: https://console.neon.tech
# Free tier: 100 CU-hours/project, 0.5GB storage, pgvector included
NEON_DATABASE_URL=
NEON_API_KEY=

# --- Upstash Redis (hot memory) ---
# Get credentials at: https://console.upstash.com
# Free tier: 256MB, 500K commands/month
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
# Legacy (for local dev with docker-compose redis):
REDIS_URL=redis://localhost:6379

# --- Langfuse (LLM observability) ---
# Get keys at: https://cloud.langfuse.com
# Free tier: 50K observations/month
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=https://cloud.langfuse.com

# --- Cube.js (semantic layer) ---
# Self-hosted or Cube Cloud: https://cube.dev
CUBE_API_URL=http://localhost:4000
CUBE_API_KEY=

# --- MCP Memory ---
MEMORY_FILE_PATH=./memory.jsonl

# --- Cloudflare Workers (set via wrangler secret put) ---
# These are managed via wrangler CLI, not .env:
# wrangler secret put NEON_DATABASE_URL
# wrangler secret put UPSTASH_REDIS_REST_URL
# wrangler secret put UPSTASH_REDIS_REST_TOKEN
# wrangler secret put ANTHROPIC_API_KEY
