{
  "name": "jade-frontier-journal",
  "version": "0.1.0",
  "description": "Jade Frontier Journal — Bilateral Learning Partnership Agent System",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "lint": "bun run tsc --noEmit",
    "check": "bun test && bun run lint",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "deploy:worker": "wrangler deploy"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^3.0.0",
    "@ai-sdk/mcp": "^1.0.0",
    "@modelcontextprotocol/sdk": "^1.27.0",
    "@neondatabase/serverless": "^1.0.0",
    "@upstash/redis": "^1.36.2",
    "ai": "^6.0.0",
    "drizzle-orm": "^0.45.0",
    "langfuse": "^3.38.6",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.0",
    "typescript": "^5.7.0"
  }
}
